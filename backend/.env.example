# Environment Settings
NODE_ENV=production
PORT=5000

# Database (Replace with your MongoDB Atlas connection string)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/expensepilot?retryWrites=true&w=majority

# JWT Secrets
JWT_ACCESS_SECRET=your_super_secret_access_key
JWT_REFRESH_SECRET=your_super_secret_refresh_key
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Frontend Origin (Replace with your deployed Vercel URL)
FRONTEND_URL=https://your-expensepilot-app.vercel.app

# Optional Redis / SMTP Configuration
REDIS_URL=
EMAIL_HOST=smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_USER=mock_user
EMAIL_PASS=mock_pass
EMAIL_FROM=noreply@expensepilot.com
