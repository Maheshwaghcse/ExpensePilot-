{
  "name": "expense-pilot-root",
  "version": "1.0.0",
  "description": "ExpensePilot Multi-Tenant Corporate Expense Pipeline Monorepo root",
  "scripts": {
    "dev": "node run-dev.js",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
